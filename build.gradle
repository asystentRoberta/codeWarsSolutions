plugins {
    id 'java'
}

version '1.0'

test {
    println 'Uruchamiam testy'
    testLogging {
        showStandardStreams = true
    }
}

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.12'
}
